<Window x:Class="SensorSimUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SensorSimUI"
        mc:Ignorable="d"
        Title="SensorSim" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10">
            <TextBlock Name="Timer" Text="{Binding ClockVm.TimeView}" Margin="5" Padding="5" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Top" />
            <TextBlock DockPanel.Dock="Right" Margin="5">
                SENSOR DATA READING 
            </TextBlock>
        </StackPanel>
        
        <StackPanel Grid.Column="0" Grid.Row="0">
            <ToolBar DataContext="{Binding SensorVm}">
                <TextBlock Text="{Binding TempText}"></TextBlock>
            </ToolBar>
            <TextBlock>Environment</TextBlock>
        </StackPanel>
    </Grid>
</Window>
