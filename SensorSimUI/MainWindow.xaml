<Window x:Class="SensorSimUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SensorSimUI"
        mc:Ignorable="d"
        Title="SensorSim" Height="450" Width="800">
    
    <Window.Resources>
        <local:ImageConverter x:Key="ImageConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10">
            <TextBlock 
                Text="{Binding ClockVm.TimeView}" 
                Margin="5" 
                Padding="5" 
                DockPanel.Dock="Right" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                />
            
            <ComboBox 
                ItemsSource="{Binding EnvironmentVm.AvailableEnvironments}"
                DisplayMemberPath="Name"
                SelectedItem="{Binding EnvironmentVm.ActiveEnvironment, Mode=TwoWay}"
                />
            
            <TextBlock DockPanel.Dock="Right" Margin="5">
                <ItemsControl ItemsSource="{Binding SensorVm.ActiveSensors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}"/>
                                <TextBlock Text=":"/>
                                <TextBlock Text="{Binding Value}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TextBlock>
        </StackPanel>
        
        <Grid  Grid.Column="0" Grid.Row="0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            
            <ToolBar Grid.Row="0" DataContext="{Binding SensorVm}" ItemsSource="{Binding AvailableSensorTypes}">
                <ToolBar.ItemTemplate>
                    <DataTemplate>
                        <!--<TextBlock
                            Margin="10, 5"
                            Background="Transparent"
                            MouseMove="SensorDrag"
                            AllowDrop="True"
                            Text="{Binding Name}"/>-->
                        <Image Width="68" Height="68" 
                               Source="{Binding SensorImage.ImagePath, Converter={StaticResource ImageConverter}}"
                               MouseMove="SensorDrag"
                               AllowDrop="True"/>
                    </DataTemplate>
                </ToolBar.ItemTemplate>
            </ToolBar>
            
            <!--Keep border element until all environment objects has image-->
            <Border Grid.Row="1" Background="{Binding EnvironmentVm.EnvironmentColor}">
                <Grid
                    AllowDrop="True"
                    Drop="Environment_Drop">
                    
                    <!--<Image Stretch="Fill" Source="{Binding EnvironmentVm.EnvImage}"/>-->
                    <Image Stretch="Fill" 
                           Source="{Binding EnvironmentVm.ActiveEnvironment.Image.ImagePath, Converter={StaticResource ImageConverter}}"/>
                    
                    <ItemsControl x:Name="dropCanvas" ItemsSource="{Binding SensorVm.ActiveSensors}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding XPosition}" />
                                <Setter Property="Canvas.Top" Value="{Binding YPosition}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="LightBlue" Padding="4" BorderBrush="DarkBlue" BorderThickness="1">
                                    <!--<TextBlock Text="{Binding Name}" />-->
                                    <Image Width="28" Height="28" 
                                           Source="{Binding SensorImage.ImagePath, Converter={StaticResource ImageConverter}}"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
