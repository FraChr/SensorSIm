<Window x:Class="SensorSimUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SensorSimUI"
        mc:Ignorable="d"
        Title="SensorSim" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10">
            <TextBlock 
                Text="{Binding ClockVm.TimeView}" Margin="5" Padding="5" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Top" />
            <TextBlock DockPanel.Dock="Right" Margin="5">
                <ItemsControl ItemsSource="{Binding SensorVm.Sensors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}"/>
                                <TextBlock Text=":"/>
                                <TextBlock Text="{Binding Temperature}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TextBlock>
        </StackPanel>
        
        <Grid  Grid.Column="0" Grid.Row="0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <ToolBar Grid.Row="0" DataContext="{Binding SensorVm}">
                <TextBlock 
                    Background="Transparent"
                    MouseMove="Sensor1_MouseMove"
                    AllowDrop="True"
                    Text="{Binding TempName}"/>
            </ToolBar>
            <Border Grid.Row="1" Background="{Binding EnvironmentVm.EnvironmentColor}" 
                    
                    AllowDrop="True"
                    Drop="Environment_Drop">
                <Canvas x:Name="dropCanvas" />
            </Border>
        </Grid>
    </Grid>
</Window>
